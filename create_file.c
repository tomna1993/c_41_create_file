#include <cs50.h>
#include <stdio.h>

int main(void)
{
	// Get the user's name

	// Get user's address

	// Check if file exist

	// If the file doesn't exist; create it

	// If file exists write the data into it
}